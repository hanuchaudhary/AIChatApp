import AvatarSelection from "./AvatarSelection"

export const Profile = () => {
    return (
        <div className="max-w-5xl mx-auto w-full p-4 bg-secondary flex items-center justify-center h-screen"> 
        <AvatarSelection/>
        </div>
    )
    }